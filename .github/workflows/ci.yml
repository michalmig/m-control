name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    name: Typecheck, Lint, Build, Smoke Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build core
        run: yarn workspace @m-control/core build

      - name: Typecheck
        run: yarn typecheck

      - name: Lint
        run: yarn lint

      - name: Test
        run: yarn test

      - name: Build
        run: yarn build

      - name: Smoke test
        run: |
          node apps/mctl/dist/bundle/index.js --help
          node apps/mctl/dist/bundle/index.js init
          node apps/mctl/dist/bundle/index.js list
          node apps/mctl/dist/bundle/index.js doctor
          node apps/mctl/dist/bundle/index.js run hello-world name=CI
          node apps/mctl/dist/bundle/index.js run hello-python name=CI
