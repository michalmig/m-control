{
  "name": "@m-control/mctl",
  "version": "0.1.0",
  "description": "m-control CLI — mctl command",
  "main": "dist/index.js",
  "bin": {
    "mctl": "./dist/index.js",
    "mm": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && ncc build dist/index.js -o dist/bundle --minify",
    "dev": "ts-node -r tsconfig-paths/register src/index.ts",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write 'src/**/*.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@m-control/core": "*"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@vercel/ncc": "^0.38.3",
    "ts-node": "^10.9.2"
  },
  "license": "MIT"
}
